[pytest]
testpaths = tests
addopts = -q

markers =
    unit: hermetic, fast, import-safe tests (PR CI default)
    integration: uses I/O, large deps, or external artifacts (nightly or optional CI job)
    e2e: full CLI flows (nightly only)
