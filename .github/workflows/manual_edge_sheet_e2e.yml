name: manual_edge_sheet_e2e.yml
on:
  workflow_dispatch:
    inputs:
      day:
        description: "YYYY-MM-DD; blank = tomorrow UTC"
        required: false
        type: string
# ... keep the current, known-good body you just restored ...
