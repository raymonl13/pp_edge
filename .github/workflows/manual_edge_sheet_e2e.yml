name: manual_edge_sheet_e2e
on:
  push:
    branches: [ "main" ]
concurrency:
  group: manual_edge_sheet_e2e-${{ github.ref }}
  cancel-in-progress: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow parses and job starts"
