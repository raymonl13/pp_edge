name: .github/workflows/manual_edge_sheet_v3.yml
on:
  workflow_dispatch: {}
jobs:
  edge_sheet:
    runs-on: ubuntu-latest
    steps:
      - name: Minimal probe
        run: echo "v3 dispatcher OK"
