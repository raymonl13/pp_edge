name: nightly_edge_sheet

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'     # 08:00 UTC daily

jobs:
  edge-sheet:
    runs-on: ubuntu-latest
    steps:
      - name: Echo hello
        run: echo "nightly bootstrap OK"
